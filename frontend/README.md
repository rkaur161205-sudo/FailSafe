# FailSafe — Frontend

React + Vite frontend for the FailSafe Student Risk Intelligence Platform.

See the root [README](../README.md) for full project documentation and setup instructions.

## Development

```bash
npm install
npm run dev
```

Runs at `http://localhost:5173`. Requires the FastAPI backend running at `http://localhost:8000`.
